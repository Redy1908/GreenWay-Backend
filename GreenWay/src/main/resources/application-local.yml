server:
  port: 8080

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8090/realms/GreenWay/protocol/openid-connect/certs
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/postgis
    username: postgis
    password: postgis
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.spatial.dialect.postgis.PostgisPG10Dialect
    show-sql: true
    database: postgresql
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
osrm:
  lon-min: 13.7
  lon-max: 15.00
  lat-min: 40.5
  lat-max: 41.5
  route-url: http://localhost:5000/route/v1/driving/
  table-url: http://localhost:5000/table/v1/driving/
opentopodata:
  url: http://localhost:5001/v1/greenway
